FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY common ./common
COPY modules ./modules
CMD ["uvicorn", "modules.travel.server:app", "--host", "0.0.0.0", "--port", "8000"]
